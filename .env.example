# Copy this file to `.env` for local development.
# Keep `.env` out of git because it contains real secrets.
# These values are examples; change ADMIN_SECRET in your local `.env`.
# .env is already ignored by git, so it will stay local.
PORT=3000
NODE_ENV=development
ADMIN_SECRET=change-this-dev-secret
